import { Badge, Box } from "@mui/material";
import styled from "styled-components";

export const CustomizeBadge = styled(Badge)`
  font-size: 0.5rem !important;
  font-weight: 100;
  padding: 0 4px
`;

export const BoxCustomize = styled(Box)`
  margin-right: 20px;
`;
