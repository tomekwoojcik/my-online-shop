import { apiBase } from "./apiSignIn";

interface loginDataModel {
    username: string,
    password:string,
}

export const signUp = async (url:string, ) => {
    try {
      
  } catch (error) {
    console.log(error);
  }
};
